[package]
name = "taskflow"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
sqlx = { version = "0.8.6", features = ["postgres", "uuid", "chrono", "macros", "migrate", "tls-rustls"] }
tokio = { version = "1.51.1", features = ["full"] }
