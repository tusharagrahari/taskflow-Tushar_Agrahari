meta {
  name: Update Task
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/tasks/{{task_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "in_progress",
    "priority": "medium"
  }
}
