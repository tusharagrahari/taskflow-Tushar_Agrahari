meta {
  name: List Tasks
  type: http
  seq: 1
}

get {
  url: {{base_url}}/projects/{{project_id}}/tasks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

params:query {
  ~status: todo
  ~assignee:
}
