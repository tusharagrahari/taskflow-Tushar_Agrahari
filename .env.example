# Database
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Connection string for sqlx
# Local dev (API outside Docker): use localhost
DATABASE_URL=postgres://<user>:<password>@localhost:5432/<db>
# Inside Docker (set automatically by docker-compose.yml, no need to change)

# JWT
JWT_SECRET=

# Server
SERVER_PORT=8080
